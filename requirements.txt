# Existing requirements
numpy
requests
pandas
Pillow
simsimd
stringzilla
usearch
fastapi

datasets  # HuggingFace datasets to fetch Wikipedia and pre-computed Arxiv
mwparserfromhell  # To parse Wikipedia articles
apache_beam  # For parsing runners

# Complex dependencies:
pyarrow
uform
transformers

# Interactive editing
ipykernel

# Dependency with specific version
encode==0.0.0
idna==2.10  # Downgraded to avoid conflicts with newer idna versions

torch
tqdm
